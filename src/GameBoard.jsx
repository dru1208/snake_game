import React from "react"

const GameBoard = () => {
  return (
    <div>
      <canvas
        id="snakeBoard"
        width="700"
        height="700"
      > 
        The Game Board for Snake!
      </canvas>
    </div>
  )
}

export default GameBoard